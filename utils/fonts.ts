import { Unbounded } from 'next/font/google';
import { Montserrat } from 'next/font/google';

export const unbounded = Unbounded({ subsets: ['cyrillic'] });
export const montserrat = Montserrat({ subsets: ['latin'] })
